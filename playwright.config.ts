import { createprojectConfig } from "project-agent-playwright-config/config";

export default createprojectConfig({
  // Add your custom playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:3000',
  // },
});
